# Astra v3 Worker Repo

This repository is meant to be connected to an existing Cloudflare Worker named `astra-v3-worker` via Settings -> Builds -> Connect.

Files:
- `src/index.js` — Worker code
- `wrangler.jsonc` — Worker configuration
